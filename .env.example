# Configure this in Apache/PHP or the process environment. This project does
# not load .env files automatically and never exposes the value to JavaScript.
HUGGINGFACE_API_TOKEN=hf_replace_with_server_side_token
